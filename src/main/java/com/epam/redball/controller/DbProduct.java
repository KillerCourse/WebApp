package com.epam.redball.controller;

public class DbProduct {
//    CREATE DATABASE 'db1';
    // use 'db1'
    /*CREATE TABLE `firstschema`.`product` (
            `id` INT NOT NULL,
            `diameter` DOUBLE NOT NULL,
            `weight` DOUBLE NOT NULL,
            `quantity_in_stock` INT NOT NULL,
            `price_per_unit` DOUBLE NOT NULL,
            `name` VARCHAR(45) NOT NULL,
            `description` VARCHAR(200) NOT NULL,
            PRIMARY KEY (`id`));*/

    // INSERT Products(diameter, weight, quantity_in_stock, price_per_unit, name, description)
    // VALUES (18, 32, 23, 50, eighty, someDescription)
    // VALUES (19, 34, 25, 55, ninety, someDescription2)в
}
